server:
  port: 8080

spring:
  application:
    name: media-tracker-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/media_app}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:localpassword123}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      leak-detection-threshold: 2000
  threads:
    virtual:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          lob:
            non_contextual_creation: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration

app:
  jwt:
    secret: ${NEXTAUTH_SECRET:6fR+w7BvNqT5zX9kM2pY1vL8sK4jD0mA3gH7fK9sJ5w=}
  gateway:
    secret: ${INTERNAL_GATEWAY_SECRET:default-internal-secret-change-in-prod-123456}
  providers:
    tmdb:
      api-key: ${TMDB_API_KEY:34ff2df714dec461e40cc4bae1933672}
    twitch:
      client-id: ${TWITCH_CLIENT_ID:oy55fz44hkvyoe3to1tkbjjqq4r0se}
      client-secret: ${TWITCH_CLIENT_SECRET:ai8ne80tuq1eqemrxefcbo0wg3fhfw}
    rawg:
      api-key: ${RAWG_API_KEY:122ec61c4d864b9c851d6ccf7a0beac9}

management:
  endpoints:
    web:
      exposure:
        include: health,info
